#include <stdio.h>

int validateName(char name[]);  // Function Declaration to validate name
int validatePhone(char phone[]); // Function Declaration to validate phone
int validateEmail(char email[]); // Function Declaration to validate email